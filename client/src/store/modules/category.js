// import Axios from "axios";

const state = {
  category: ["Figma", "Funko", "Nendoroid", "PPP"],
};
const mutations = {};
const actions = {};
const getters = {
  allCategory: (state) => state.category,
};

export default {
  state,
  mutations,
  actions,
  getters,
};
